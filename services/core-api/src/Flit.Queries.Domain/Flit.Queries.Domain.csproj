<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Library</OutputType>
    <RootNamespace>Flit.Queries.Domain</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- HU #12321 — TenantScope.All() es internal: solo el middleware (Flit.Api) y los tests lo fabrican. -->
    <InternalsVisibleTo Include="Flit.Api" />
    <InternalsVisibleTo Include="Flit.Admin.Tests" />
    <InternalsVisibleTo Include="Flit.Infrastructure.Tests" />
  </ItemGroup>

</Project>
