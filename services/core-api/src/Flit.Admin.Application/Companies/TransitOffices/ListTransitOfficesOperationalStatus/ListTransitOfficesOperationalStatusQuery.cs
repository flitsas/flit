namespace Flit.Admin.Application.Companies.TransitOffices.ListTransitOfficesOperationalStatus;

/// <summary>
/// Consulta del estado operativo de los organismos de tránsito (RF01). Sin parámetros:
/// el filtrado/búsqueda se resuelve en el cliente sobre el catálogo completo (mismo
/// patrón que el listado de catálogo OT).
/// </summary>
public sealed record ListTransitOfficesOperationalStatusQuery;
