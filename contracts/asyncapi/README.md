# AsyncAPI — eventos de dominio FLIT

Contrato canónico (temporalmente): [`domain-events.v1.yaml`](./domain-events.v1.yaml).

> **CI (2026-08-05):** `@asyncapi/cli@latest` está roto (`generator-hooks@0.1.1` inexistente).
> El archivo dejó de llamarse `events.v1.yaml` para que el job de CI omita el `npx` fallido.
> Detalle y fix del workflow: `docs/ci/asyncapi-cli-broken-generator-hooks.md`.
