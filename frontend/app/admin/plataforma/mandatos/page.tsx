import { notFound } from "next/navigation";

/**
 * Placeholder SuperAdmin — Plataforma → Mandatos.
 * Módulo aún no construido: dispara el `not-found` FLIT del segmento.
 */
export default function AdminMandatosPage() {
  notFound();
}
